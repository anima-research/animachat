node_modules/
dist/
.env
.env.local
.env.bak*
*.log
.DS_Store

# Database persistence
data/
*.jsonl

# IDE
.vscode/
.idea/

# LLM logs
logs/

# SSL certificates
certs/
*.pem
*.key
*.crt
*.csr

# Site configuration (deployment-specific)
backend/config/siteConfig.json
backend/config/siteConfig.production.json

# Scratch markdown / text files only — track real docs (README.md, AGENTS.md,
# CHANGELOG.md, etc.). Original blanket `*.md` and `*.txt` patterns silently
# blocked all documentation from being added to the repo.
scratch-*.md
scratch-*.txt
.notes/
